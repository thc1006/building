# 🎬 Isaac Sim 建模與動畫腳本使用指南

赤土崎全齡社福樞紐 - 完整實作工作流程

---

## 📋 腳本總覽

本專案提供 **9 個 Python 腳本**，涵蓋從建模到動畫的完整流程。

### 建模腳本（Blender）

| 腳本 | 功能 | 輸出 |
|------|------|------|
| `blender_generate_B1.py` | B1 停車場+設備層 | `floor_B1.usdc` |
| `blender_generate_1F.py` | 1F 長照日照中心 | `floor_1F.usdc` |
| `blender_generate_2F.py` | 2F 公共托嬰中心 | `floor_2F.usdc` |
| `blender_generate_3F.py` | 3F 家庭支持服務中心 | `floor_3F.usdc` |
| `blender_generate_4F.py` | 4F 青少年活動中心 | `floor_4F.usdc` |

### 場景組裝與動畫腳本（Isaac Sim）

| 腳本 | 功能 | 說明 |
|------|------|------|
| `assemble_complete_building.py` | 組裝完整建築 | 載入所有樓層，設定照明、攝影機、物理 |
| `animate_elevators.py` | 電梯動畫 | 電梯箱體建立、樓層移動、門開關 |
| `animate_doors.py` | 門開關動畫 | 滑動門、推門、雙開門自動開關 |
| `animate_people.py` | 人員移動動畫 | 長者、幼兒、青少年、工作人員移動 |

---

## 🚀 完整工作流程

### 階段 1: 建立各樓層模型（使用 Blender）

#### 步驟 1.1: 執行建模腳本

```bash
# 建立所有樓層（依序執行）
blender --background --python scripts/blender_generate_B1.py
blender --background --python scripts/blender_generate_1F.py
blender --background --python scripts/blender_generate_2F.py
blender --background --python scripts/blender_generate_3F.py
blender --background --python scripts/blender_generate_4F.py
```

**或在 Blender GUI 中執行**:
1. 開啟 Blender
2. Scripting 工作區
3. Open → 選擇腳本 → Run Script
4. File → Export → USD (.usdc)
5. 儲存至 `/home/user/building/assets/floor_XX.usdc`

#### 步驟 1.2: 驗證 USD 檔案

```bash
# 檢查是否所有樓層都已匯出
ls -lh assets/*.usdc

# 應該看到5個檔案：
# floor_B1.usdc
# floor_1F.usdc
# floor_2F.usdc
# floor_3F.usdc
# floor_4F.usdc
```

**預期結果**:
- 每個 `.usdc` 檔案大小約 1-5 MB
- 可在 usdview 或 Isaac Sim 中開啟預覽

---

### 階段 2: 組裝完整建築（使用 Isaac Sim）

#### 步驟 2.1: 執行場景組裝腳本

**方法 A: Isaac Sim Script Editor**

1. 開啟 Isaac Sim
2. Window → Script Editor
3. File → Open Script → 選擇 `assemble_complete_building.py`
4. 點擊 Run (▶️)

**方法 B: 命令列執行**

```bash
~/.local/share/ov/pkg/isaac_sim-2023.1.1/isaac-sim.sh \
  --exec /home/user/building/scripts/assemble_complete_building.py
```

#### 步驟 2.2: 檢視組裝結果

1. 開啟 Isaac Sim
2. File → Open → `/home/user/building/scenes/complete_building.usd`
3. 場景包含：
   - ✅ 5 層樓（B1-4F）疊加
   - ✅ 電梯井
   - ✅ 環境照明（太陽光 + 環境光）
   - ✅ 6 個攝影機視角
   - ✅ 物理模擬設定

#### 步驟 2.3: 調整視角

在 Isaac Sim 中:
- **Viewport → Camera** → 選擇攝影機:
  - `Overview_Aerial`: 鳥瞰全景
  - `Front_Entrance`: 1F 正面入口
  - `1F_Interior`: 1F 內部
  - `2F_Nursery`: 2F 托嬰中心
  - `4F_Basketball`: 4F 籃球場
  - `Side_Elevation`: 側面立面

---

### 階段 3: 添加動畫（使用 Isaac Sim）

#### 動畫 1: 電梯移動

```bash
# 在 Isaac Sim Script Editor 中執行
# 或命令列執行
~/.local/share/ov/pkg/isaac_sim-2023.1.1/isaac-sim.sh \
  --exec /home/user/building/scripts/animate_elevators.py
```

**功能**:
- 建立 2 座電梯箱體（不鏽鋼材質）
- 電梯1路線: B1 → 1F → 2F → 3F → 4F → ... → B1
- 電梯2路線: 1F → 4F → 1F（快速往返）
- 電梯門自動開關動畫

**播放**:
1. 重新載入場景
2. 按 Play (▶️)
3. 觀察電梯在樓層間移動

---

#### 動畫 2: 門開關

```bash
# 執行門開關動畫腳本
~/.local/share/ov/pkg/isaac_sim-2023.1.1/isaac-sim.sh \
  --exec /home/user/building/scripts/animate_doors.py
```

**功能**:
- **1F 主入口**: 自動滑動門（雙開，玻璃材質）
- **2F 托嬰入口**: 推門（木質）
- **3F 諮商室**: 滑動門
- **4F 籃球場**: 雙開推門

**動畫週期**: 10 秒循環（開門 2s → 停留 6s → 關門 2s）

---

#### 動畫 3: 人員移動

```bash
# 執行人員移動動畫腳本
~/.local/share/ov/pkg/isaac_sim-2023.1.1/isaac-sim.sh \
  --exec /home/user/building/scripts/animate_people.py
```

**功能**:
- **1F**: 2位長者在庭園散步
- **2F**: 3位幼兒在遊戲區活動
- **4F**: 4位青少年打籃球
- **全館**: 2位工作人員巡視

**人物模型**: 簡化圓柱體 + 球體（可替換為 NVIDIA Character Generator 真實人物）

**動畫時長**: 30 秒循環

---

## 🎨 渲染與匯出

### 設定渲染品質

在 Isaac Sim 中:
1. **Render Settings** (右側面板)
2. Renderer: **Path Tracing** (最高品質)
3. Samples per Pixel: **256+**
4. Max Bounces: **8**
5. Denoiser: **ON**

### 匯出高品質圖片

```
Window → Utilities → Movie Capture

設定:
- Frame Range: 0-900 (30秒 @ 30fps)
- Resolution: 1920×1080 或 3840×2160
- Format: PNG (圖片序列) 或 MP4 (影片)
- Output Path: /home/user/building/renders/
```

### 推薦渲染視角

| 視角 | 攝影機 | 用途 |
|------|--------|------|
| 建築全景 | `Overview_Aerial` | 展示整體建築 |
| 1F 內部 | `1F_Interior` | 展示長照設施 |
| 電梯動畫 | 自訂（追蹤電梯） | 展示垂直動線 |
| 人員活動 | `2F_Nursery` | 展示使用情境 |

---

## 📊 腳本詳細說明

### 1. `blender_generate_B1.py` - B1 地下層

**建立內容**:
- 停車區域：30 個車位（一般 20 + 無障礙 5 + 親子 5）
- 空調機房、配電室、給排水設備、倉儲空間
- 電梯井、安全梯
- 區域照明（冷白光）

**關鍵參數**:
```python
FLOOR_WIDTH = 30.0  # m
FLOOR_LENGTH = 20.0  # m
FLOOR_HEIGHT = 3.5  # m
```

---

### 2. `blender_generate_1F.py` - 1F 長照日照中心

**建立內容**:
- 失智專區（3 個房間，200 m²）
- 一般日照區（3 個房間，300 m²）
- 共用餐廳、備餐廚房、無障礙庭園
- 護理站、無障礙廁所

**特殊設計**:
- 隔音牆 (STC 65)
- 柔和照明（適合長者）
- 暖色系地板材質

---

### 3. `blender_generate_2F.py` - 2F 公共托嬰中心

**建立內容**:
- 嬰兒室 0-1歲（15 張嬰兒床）
- 幼兒室 1-2歲（遊戲區、午睡室、閱讀角）
- 戶外遊戲區（85 m²）
- 幼兒餐廳、備餐區

**特殊模型**:
- `create_crib()` 函數建立嬰兒床
- 戶外圍欄（透明壓克力）
- 10 張幼兒餐桌

---

### 4. `blender_generate_3F.py` - 3F 家庭支持服務中心

**建立內容**:
- 諮商服務區（4 間個別諮商室 + 家族治療室 + 遊戲治療室）
- 多功能教室（100 m²，50 人座）
- 親子烹飪教室（中島工作台 2 組）
- 社區共餐廚房、志工培訓室

**家具模型**:
- 50 張教室桌椅
- 2 組烹飪中島

---

### 5. `blender_generate_4F.py` - 4F 青少年活動中心

**建立內容**:
- 室內籃球場（15m × 10m，天花板高 6m，含 2 個籃框）
- 舞蹈教室、體適能區
- 自習室（40 個座位）
- 電腦教室、創客空間
- 交誼廳（3 組沙發）、團練室

**特殊設計**:
- 籃球場高照明（800W × 6）
- 木質地板材質
- 40 張自習桌（自動生成）

---

### 6. `assemble_complete_building.py` - 場景組裝

**功能**:
1. 載入所有樓層 USD 檔案
2. 設定正確的樓層高度（Z 軸位移）
3. 建立電梯井（貫穿所有樓層）
4. 添加環境照明（太陽光 + Dome Light）
5. 建立 6 個攝影機視角
6. 啟用物理模擬（重力、碰撞）
7. 添加場景元數據（建築資訊）

**樓層高度配置**:
```python
FLOOR_HEIGHTS = {
    "B1": -3.5,   # 地下
    "1F": 0.0,    # 地面層
    "2F": 4.0,
    "3F": 7.8,
    "4F": 11.3,
}
```

---

### 7. `animate_elevators.py` - 電梯動畫

**功能**:
1. `create_elevator_car()`: 建立電梯箱體（地板、天花板、四面牆、雙開門）
2. `animate_elevator_movement()`: 電梯移動動畫（根據樓層序列）
3. `animate_elevator_doors()`: 門開關動畫（滑動）

**動畫參數**:
```python
ELEVATOR_SPEED = 1.0  # m/s
DOOR_OPEN_TIME = 2.0  # s
DOOR_CLOSE_TIME = 2.0  # s
STOP_DURATION = 5.0  # s
```

**電梯路線**:
- 電梯1: B1 → 1F → 2F → 3F → 4F → ... → B1（完整巡迴）
- 電梯2: 1F → 4F → 1F（快速往返）

---

### 8. `animate_doors.py` - 門開關動畫

**門的類型**:
- **滑動門** (`sliding`): 雙門向兩側滑動
- **推門** (`push`): 單門旋轉開啟
- **雙開推門** (`double_push`): 雙門向兩側旋轉

**動畫邏輯**:
```
0s: 門關閉
→ 2s: 門開啟
→ 8s: 保持開啟
→ 10s: 門關閉（循環）
```

**材質**:
- 滑動門: 玻璃（透明度 0.3）
- 推門: 木質

---

### 9. `animate_people.py` - 人員移動動畫

**人物模型**:
- **簡化版**: 圓柱體（身體）+ 球體（頭部）
- **尺寸**: 根據年齡調整（長者 1.65m, 成人 1.70m, 幼兒 1.0m, 青少年 1.60m）
- **顏色**: 不同族群使用不同顏色區分

**動畫場景**:
1. **1F**: 長者庭園散步（速度 0.6 m/s）
2. **2F**: 幼兒遊戲區活動（速度 2.0 m/s）
3. **4F**: 青少年打籃球（來回移動）
4. **全館**: 工作人員巡視（護理師、教保員）

**行走動畫**:
- `animate_walk()` 函數：根據路徑點（waypoints）建立關鍵影格
- 自動計算移動時間（距離 / 速度）

---

## 🛠️ 故障排除

### 問題 1: Blender 腳本執行後沒有模型

**解決方案**:
1. 檢查 Console 是否有錯誤訊息（Window → Toggle System Console）
2. 確認 Blender 版本 ≥ 3.6
3. 按 Home 鍵全畫面顯示
4. 切換到 Camera 視角（數字鍵 0）

---

### 問題 2: USD 匯出後在 Isaac Sim 中打不開

**解決方案**:
1. 確認匯出格式為 **.usdc** (Binary)
2. 檢查檔案大小（不應為 0 bytes）
3. 使用 `usdview` 預覽檔案:
   ```bash
   usdview assets/floor_1F.usdc
   ```

---

### 問題 3: assemble_complete_building.py 找不到樓層檔案

**錯誤訊息**:
```
⚠️ 警告: USD 檔案不存在: /home/user/building/assets/floor_1F.usdc
```

**解決方案**:
1. 確認所有 Blender 腳本已執行並匯出 USD
2. 檢查檔案路徑：
   ```bash
   ls -l assets/
   ```
3. 如果缺少某層，重新執行該層的 Blender 腳本

---

### 問題 4: 動畫播放時沒有移動

**解決方案**:
1. 確認 Timeline 已設定正確範圍（例如 0-900 影格）
2. 按 Play 按鈕（▶️），不是 Step Forward
3. 檢查物件是否有動畫（Stage → 選擇物件 → 查看屬性面板）
4. 重新載入場景

---

### 問題 5: 渲染速度很慢

**解決方案**:
1. 降低 Samples per Pixel（256 → 128）
2. 使用 RTX Real-Time 渲染模式（較低品質但快速）
3. 減少場景複雜度（暫時隱藏某些樓層）
4. 使用較低解析度（1280×720）

---

## 📈 進階擴展

### 擴展 1: 使用 NVIDIA Character Generator 建立真實人物

1. 訪問: https://www.nvidia.com/en-us/omniverse/apps/character-creator/
2. 建立角色（長者、幼兒、青少年、工作人員）
3. 匯出為 USD 格式
4. 在 `animate_people.py` 中替換簡化模型:
   ```python
   # 替換 create_person() 函數
   def create_person(stage, person_id, person_type, position):
       character_usd = f"/path/to/characters/{person_type}.usd"
       person_path = f"/Building/People/{person_id}"
       person_ref = stage.DefinePrim(person_path)
       person_ref.GetReferences().AddReference(character_usd)
       # 設定位置...
   ```

---

### 擴展 2: 添加家具細節

**方法 A: 手動添加**
1. 使用 Blender Asset Browser 或 Sketchfab 下載家具模型
2. 在 Blender 腳本中 Import 模型:
   ```python
   bpy.ops.import_scene.gltf(filepath="/path/to/chair.glb")
   ```

**方法 B: 使用 NVIDIA Omniverse 素材庫**
1. 在 Isaac Sim 中開啟場景
2. Content → NVIDIA Assets
3. 拖曳家具到場景中

---

### 擴展 3: 添加日夜光照變化

```python
# 在 assemble_complete_building.py 中添加
def animate_sun_rotation(stage, duration=60):
    """太陽光旋轉動畫（模擬日夜）"""
    sun_path = "/Environment/SunLight"
    sun_xform = UsdGeom.Xformable(stage.GetPrimAtPath(sun_path))

    rotate_op = sun_xform.AddRotateXYZOp()

    # 早晨
    rotate_op.Set(Gf.Vec3f(-10, 135, 0), time=0)
    # 中午
    rotate_op.Set(Gf.Vec3f(-90, 135, 0), time=int(duration * FPS / 2))
    # 傍晚
    rotate_op.Set(Gf.Vec3f(-170, 135, 0), time=int(duration * FPS))
```

---

### 擴展 4: VR 虛擬實境導覽

1. 啟用 Isaac Sim VR 模式:
   ```
   Window → Extensions → 搜尋 "VR"
   啟用 omni.kit.vr
   ```

2. 連接 VR 頭戴裝置（HTC Vive, Oculus Rift 等）

3. 按 VR 按鈕進入虛擬實境

4. 使用手把控制移動和互動

---

## 🎯 推薦工作流程

### 初學者（第一次使用）

1. ✅ 先執行 **1 個樓層**（例如 1F）
2. ✅ 驗證 Blender → USD → Isaac Sim 流程
3. ✅ 調整材質和照明
4. ✅ 熟悉後再建立其他樓層

---

### 中級使用者（已熟悉基本操作）

1. ✅ 批量執行所有 Blender 腳本
2. ✅ 執行 `assemble_complete_building.py` 組裝
3. ✅ 添加電梯和門動畫
4. ✅ 渲染靜態圖片（每層樓 3-5 張）

---

### 進階使用者（完整專案）

1. ✅ 建立所有樓層並組裝
2. ✅ 添加所有動畫（電梯、門、人員）
3. ✅ 使用真實人物模型（Character Generator）
4. ✅ 製作 3-5 分鐘導覽影片
5. ✅ 產出 VR 虛擬實境體驗

---

## 📞 技術支援

### 遇到問題？

1. **查閱文檔**:
   - `QUICKSTART.md` - 快速入門
   - `isaac_sim_implementation_plan.md` - 完整技術文檔

2. **社群論壇**:
   - [NVIDIA Omniverse 論壇](https://forums.developer.nvidia.com/c/omniverse/)
   - [Blender Artists](https://blenderartists.org/)

3. **官方文檔**:
   - [Isaac Sim 文檔](https://docs.omniverse.nvidia.com/isaacsim/latest/)
   - [USD 文檔](https://openusd.org/)

---

## 🎉 成功案例檢查清單

完成專案後，你應該擁有:

- [ ] 5 個樓層的 USD 模型檔案 (.usdc)
- [ ] 1 個完整建築場景檔案 (complete_building.usd)
- [ ] 電梯移動動畫（2 座電梯）
- [ ] 門開關動畫（4 個主要門）
- [ ] 人員移動動畫（12 個人物）
- [ ] 至少 10 張高品質渲染圖
- [ ] 1 支 3-5 分鐘導覽影片（選配）

---

**恭喜你完成了赤土崎全齡社福樞紐的 3D 建模與動畫專案！** 🎊

有任何問題或改進建議，歡迎隨時提出。

*最後更新: 2025-11-23*
